% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diffview.R
\name{diffview}
\alias{diffview}
\title{Compare two Rydra configuration files and display differences}
\usage{
diffview(config_path1, config_path2, ignore_whitespace = TRUE)
}
\arguments{
\item{config_path1}{Path to the first YAML configuration file.}

\item{config_path2}{Path to the second YAML configuration file.}

\item{ignore_whitespace}{Logical, whether to ignore whitespace differences.
Defaults to TRUE.}
}
\value{
A character vector containing the formatted diff output.
Each element represents a line of the diff.
}
\description{
This function compares two YAML configuration files and outputs a nicely
formatted diff, ignoring whitespace changes. It shows additions, deletions,
and modifications between the two configuration files.
}
\examples{
\dontrun{
# Create two sample config files for demonstration
config1_path <- tempfile(fileext = ".yaml")
config2_path <- tempfile(fileext = ".yaml")

# Write sample configs
writeLines(c(
  "model_name: test_model",
  "centering:",
  "  age: 30",
  "main_model:",
  "  intercepts:",
  "    baseline: 1.0"
), config1_path)

writeLines(c(
  "model_name: test_model",
  "centering:",
  "  age: 35", 
  "main_model:",
  "  intercepts:",
  "    baseline: 1.5",
  "    new_param: 0.2"
), config2_path)

# Compare the configs
diff_result <- diffview(config1_path, config2_path)
cat(diff_result, sep = "\n")

# Clean up
unlink(c(config1_path, config2_path))
}
}
